<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.krismanpratama.expertsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpertSystem">
        <activity android:name=".ui.konsultasi.KonsultasiActivity"></activity>
        <activity android:name=".ui.basispengetahuan.RulesBasisPengetahuanActivity" />
        <activity android:name=".ui.basispengetahuan.AddBasisPengetahuanActivity" />
        <activity android:name=".ui.penyakit.AddPenyakitActivity" />
        <activity android:name=".ui.gejala.AddGejalaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>